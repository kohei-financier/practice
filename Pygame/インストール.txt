パッケージの確認
python -m pip list

-m
モジュールを指定するオプション

pip
Pythonのパッケージ管理しているモジュール

パッケージ「pygame」のインストール
python -m pip install --user pygame

--user
パッケージのインストール先をホームディレクトリにする。
これにより管理者権限がなくてもパッケージがインストールできる。
